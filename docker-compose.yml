services:
  backend:
    build: ./backend
    ports:
      - "8081:8080"

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE: http://localhost:8081/api
    ports:
      - "8080:80"
    depends_on:
      - backend
